_target_: src.models.nets.text_encoder.TMRWrapperEncoder
# path: third_packages/TMR/models/tmr_kitml_guoh3dfeats

# Cache text embeddings in memory across training steps.
# Safe to enable because TMR encode_text uses sample_mean=True (deterministic, no VAE sampling).
# Speeds up training by avoiding redundant forward passes through the frozen text encoder.
# Set to false to disable caching (e.g. for debugging or if text encoder behaviour changes).
cache_text_embeddings: true